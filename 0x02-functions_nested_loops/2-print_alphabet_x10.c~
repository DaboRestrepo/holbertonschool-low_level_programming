#include "main.h"
/**
 * main - Alfabeto 10 veces.
 *
 * Return: Always 0.
 */

int main(void)
{
int a;
int b;
    
for (a = 0 ; a < 10 ; a++)
{
for (b = 97 ; b <= 122 ; b++)
_putchar(b);
_putchar('\n'); 
}
return 0;
}
